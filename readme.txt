2 pipelines are used.
frontend react to deploy to s3
serverapi to build test deploy lambda and api g/w
the project is structured into server side (api and lambda) and frontend react folders.

project32
| 
|-frontEnd
  readme.txt description
|-serverApi
  readme.txt
  serverless.yml
  buildspec.yml
|--test
